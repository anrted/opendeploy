# OpenDeploy web UI

Vue 3/Vite/Pinia single-page application embedded into `opendeploy-core` for
production. See the repository [README](../README.md), [testing
guide](../TESTING.md) and [audit](../AUDIT.md).

```bash
npm ci
npm test
npx eslint . --max-warnings 0
npm run build
```
